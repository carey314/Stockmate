# 2026-09-14 Web / server 正式发布

授权：Carey 明确要求“提交代码到GitHub，并部署，我要测试”。本次范围为当前已完成的 Web 和共享后端；不提交 App 工作区、私有凭据、用户库、上传数据、模拟器产物或含用户数据的证据文件。

## 发布前核验

- GitHub `carey314/Stockmate`，main 与 origin/main 起点一致。
- 生产 `/opt/stockmate/server/prisma/prod.db` 仍为 26 业务表旧基线，完整性 ok；PM2 `stockmate-server`。
- 唯一升级路径：本目录 `evidence/production-platform-upgrade.sql`，SHA256 `1661ace91afa25be5ec51b727b8500013fffaefe878839009563dbf0df189d73`。当前 Prisma schema 与生产副本演练目标哈希一致。
- Web 21 文件 174 项通过；后端明确 `test/*.test.js` 范围 351 项通过、0 失败、0 跳过。首次调度误用自动发现，将三个 IPC worker 当独立测试执行失败；已改为项目正式文件范围，未为调度错误修改业务或 worker。
- 正式 Web 构建通过，`VITE_BASE=/mate/`、`VITE_API_BASE=https://qxju.shop/mate-api/api/v1`。仅现有大 chunk 提示。
- `deploy-additive-upgrade.py` 在私有副本验证成功升级及故意错目标回滚，均通过。脚本核对基线、SQL 哈希、旧值/类型/序列、精确目标 schema、integrity/FK，提交前异常回滚；不启动服务、不访问第三方。

## 执行顺序

1. 推送本次 Web/server 代码；使用已提交文件构建服务端发行包，Linux 独立目录安装依赖与生成 Prisma 客户端。
2. PM2 停写后将数据库一致 backup、旧 server（含配置/Prisma 客户端）、Web、uploads、PM2/nginx 配置备份至主机 0700 私有目录，文件 0600。核对备份内容可恢复。
3. 单事务执行完整 SQL，保留旧业务值；不运行 seed、db push 或旧 migrate deploy，不伪造迁移历史。
4. 保留商家 JWT 与现有服务配置，安全补独立平台 JWT/AES 密钥。通过离线 CLI 创建 Carey 平台账号，密码仅写本机私有交付文件，不进日志/GitHub。
5. 替换匹配的后端/Prisma 客户端及正式 Web，启动同一 PM2 服务；核对公网健康、能力、鉴权、平台只读页面/API，以及静态资源 SHA256。
6. 不自动发短信、发起 Apple 购买或调用 AI；已有配置保留，真实业务验收与缺项单列。

## 回退边界

DDL 提交前失败由脚本同连接 ROLLBACK。未开放服务前可恢复维护备份和旧发行包；若已开放写入，禁止用旧库覆盖新单据/体验码/用量。先停写并备份当刻库，优先向前修复或只回退兼容代码。副本事务回滚不等于已验证生产服务回退。

## 最终状态：生产已部署并验收

- GitHub main 已推送代码 `a5d10f376a7c6d680ebe456bae961690cb212995`。生产发行目录 `/opt/stockmate/releases/a5d10f376a7c-20260914`；回执另行提交，不要求重新构建业务包。
- 私有维护备份 `/opt/stockmate/backups/releases/a5d10f376a7c-20260914`：`prod.db` 为停写后的 SQLite 一致 backup；`before.tar` 保存旧后端、配置、依赖、上传目录、Web 和 nginx；归档已完整读取，uploads/Web 内容逐文件哈希匹配。
- 单事务添加式升级已提交：26旧业务表及序列的原字段/值/类型保持，17新增表；精确目标 schema 一致、integrity ok、无新增外键异常。未修改历史业务值。
- PM2 `stockmate-server` 已恢复 online，实际打开原 `/opt/stockmate/server/prisma/prod.db`。89个后端源文件/配置结构文件 SHA256 匹配已提交源码，原 `.env` 内容保持，仅追加平台私有配置。平台账号通过离线 CLI 创建成功。
- 公网 `/mate-api/health`、平台真实登录/profile/overview/users/单用户详情/AI明细/码列表/审计均 HTTP200；无身份请求及平台JWT访问商家身份/兑换均401。没有获取或伪造商家登录令牌。
- 公网50个Web文件与本地正式构建 SHA256 全相同，`/mate/platform` 与 `/mate/experience-code` history fallback 正常。
- 独立浏览器真实表单登录 → 总览 → 用户 → AI → 体验码监管 → 退出通过，无页面脚本异常；等待监管数据加载后截图并实看。首次浏览器导航未完成；另次诊断页面无请求/脚本错误，后续完整验收通过，未因此修改业务。
- 未生成或兑换生产体验码、未改变任何商家订阅；真实店铺兑换留给 Carey 在正式页面操作。仅新增独立平台账号及其登录/创建审计。
- 生产扫码能力 enabled=true；Apple两阶段身份能力 enabled=true（不等于已验证真实Apple授权或购买）；SMS enabled=false，未把本机PNVS凭据复制到生产，未实际发送/核验短信。正式SK2购买仍需APPLE_APP_ID等外部条件核验。
- App源码/安装包/生产政策页面未在此次发布中修改。本机3100/5180及其他联验服务未切换；测试请使用正式URL。

正式平台入口：`https://qxju.shop/mate/platform`。先进入“体验码与审计”生成正式环境的新码，再用店铺老板身份打开 `https://qxju.shop/mate/experience-code` 领取；本地合成库的码未复制到生产。

脱敏机器证据：`evidence/production-release-20260914.json`。详细运行日志、包含生产汇总的截图、凭据文件只留用户本机0700私有目录，不提交GitHub。历史逐用户token/首次渠道缺项不会补造，新调用开始记录。
